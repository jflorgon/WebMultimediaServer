---
description: Rebuild and restart Docker containers after code changes
---

Run `python docker-start.py up --build -d` from the repo root to rebuild and restart all containers. Show the last 15 lines of output. If the build fails, show the full error.
