---
description: Tail logs from the API container
---

Run `python docker-start.py logs --tail=80 mediaserver-api` from the repo root and show the output. If the user specifies a service name in the args, use that instead of `mediaserver-api`.
